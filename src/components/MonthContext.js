//import {createContext} from "react";

// TODO: should have function to: add appointment, remove appointment, edit appointment, update next/prev month
//const initialValue = {
    //month: [],
    //setMonth: () => {console.error('This is the initial value');}
//}

//const MonthContext = createContext(initialValue);

//export default MonthContext;
